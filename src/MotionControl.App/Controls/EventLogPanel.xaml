<UserControl x:Class="MotionControl.App.Controls.EventLogPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MotionControl.App.Controls"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="800">
    <Border Style="{StaticResource SectionCardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=controls:EventLogPanel}}"
                       Style="{StaticResource SectionTitleTextStyle}" />

            <TextBlock Grid.Row="1"
                       Text="{Binding Hint, RelativeSource={RelativeSource AncestorType=controls:EventLogPanel}}"
                       Style="{StaticResource SectionHintTextStyle}" />

            <!--
                Content injection: each page puts its DataGrid (with columns) here.
                The DataGrid should use Style="{StaticResource EventLogDataGridStyle}".
                Usage:
                    <controls:EventLogPanel ...>
                        <DataGrid ItemsSource="{Binding MyEvents}" Style="{StaticResource EventLogDataGridStyle}">
                            <DataGrid.Columns>...</DataGrid.Columns>
                        </DataGrid>
                    </controls:EventLogPanel>
            -->
            <ContentControl Grid.Row="2"
                            Content="{Binding LogContent, RelativeSource={RelativeSource AncestorType=controls:EventLogPanel}}" />

        </Grid>
    </Border>
</UserControl>
